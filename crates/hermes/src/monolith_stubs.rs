//! Removido — stubs movidos para k_ai via hermes::globals.
//! Arquivo mantido vazio intencional para não quebrar `mod monolith_stubs` se referenciado.
//! Preferir: `use crate::globals::{agency, hw_agents, boot_log_agent, audit, inventory}`.
