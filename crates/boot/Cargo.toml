[package]
name = "boot"
version = "0.1.0"
edition = "2021"
publish = false

[build-dependencies]
neural-kernel = { path = "../neural-kernel", artifact = "bin", target = "x86_64-unknown-none", features = ["fat-boot-log"] }
