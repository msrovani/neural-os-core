[package]
name = "vector-db"
version = "0.1.0"
edition = "2021"
description = "ADR-0064 — VectorStore TF-IDF + cosine (RAG L1 lexical), no_std"

[dependencies]
spin = "0.9"

[lints.rust]
unexpected_cfgs = { level = "allow" }
