# Release notes — v1.9.9 TEST

**Tag:** `v1.9.9` → `a491cea`  
**Title:** v1.9.9 TEST — SGDB D/E Memory Quality

## Summary

SGDB D-series + Memory Quality (E1–E5). Bare-metal AIOS memory path: TickvLite + ART/BQ + SleepCycle checkpoint.

## Included

- Hamming dispatch (`scalar` / AVX2 LUT / AVX-512 + VPOPCNTDQ when CPUID)
- L0/L1 RAM-only + `checkpoint_working` / `prune_working_ram`
- TickvLite Valid-flag invalidate + stress GC
- Hermes recall L4 BQ hybrid; ART Node16 SSE
- NMD1 `patch_payload` + sortable `ts/` keys

## Honesty (not claimed)

- Upstream crates `tickv` / `noproto` — residual
- HNSW / DoD 10M–100k — residual
- AEAD / kill-9 HW validation — residual

## Commit message note

`a491cea` was scooped by a Cursor auto-checkpoint; **content** is the intentional D/E work. See SESSION_176 and CHANGELOG annotation. F-series (e2e smoke, SleepCycle unify, pseudo-emb, FP32 rescore) lands in a later commit on `main` (`2282e15`).

## Verify

```text
cargo check --release -p k_ai -p hermes -p neural-kernel --features fat-boot-log
```

## Create on GitHub (when `gh` available)

```powershell
gh release create v1.9.9 --title "v1.9.9 TEST — SGDB D/E Memory Quality" --notes-file docs/releases/v1.9.9.md
```
