[workspace]
resolver = "2"
members = [
    "crates/neural-kernel",
    "crates/agent-core",
    "crates/skill-registry",
    "crates/event-bus",
]
default-members = ["crates/neural-kernel"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
